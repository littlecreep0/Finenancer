<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="60dp">

    <ImageView
        android:id="@+id/bond_icon"
        android:layout_width="90dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:src="@drawable/ic_launcher_foreground" />

    <TextView
        android:id="@+id/bond_name"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/bond_icon"
        android:textAlignment="viewStart"
        android:ellipsize="end"
        android:textSize="30sp"
        tools:text="Bond" />

    <TextView
        android:id="@+id/bond_ticker"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/bond_name"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/bond_icon"
        android:textAlignment="viewStart"
        android:textSize="12sp"
        tools:text="Ticker" />

    <TextView
        android:id="@+id/bond_country"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/bond_name"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/bond_ticker"
        android:layout_marginStart="12dp"
        android:textAlignment="viewStart"
        android:textSize="12sp"
        tools:text="Country" />

    <TextView
        android:id="@+id/bond_date"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/bond_name"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/bond_country"
        android:layout_marginStart="12dp"
        android:textAlignment="viewStart"
        android:textSize="12sp"
        tools:text="Date" />
</androidx.constraintlayout.widget.ConstraintLayout>